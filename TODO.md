# TODO

## iTerm2

- Make vanilla iTerm2 splits start in the active pane’s directory (OK for new tabs to default to `$HOME`); bind keys to `split_pane_cwd_vertical()` / `split_pane_cwd_horizontal()`.
