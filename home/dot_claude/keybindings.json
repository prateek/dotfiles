{
  "$schema": "https://www.schemastore.org/claude-code-keybindings.json",
  "$docs": "Pins chat:queueSubmit to its current default chord. The karabiner meh+d macro (karabiner.edn.tmpl) types this chord to defer a draft as /q; an unpinned Claude Code default could change silently and break the macro with no diff anywhere.",
  "bindings": [
    {
      "context": "Chat",
      "bindings": {
        "ctrl+x enter": "chat:queueSubmit"
      }
    }
  ]
}
