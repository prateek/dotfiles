[".local/share/zinit/zinit.git"]
type = "git-repo"
url = "https://github.com/zdharma-continuum/zinit.git"
refreshPeriod = "168h"
{{- if (includeTemplate "features.tmpl" . | fromJson).private_overlay }}
{{/* Private overlays (internal/work-specific) composed into generated docs by
     run_after_37-agent-slack-doc. The private_overlay feature is true only on
     work in machines.toml; CI/personal/homelab and forks never reference the
     private repo. */}}
[".local/share/dotfiles/private"]
type = "git-repo"
url = "git@github.com:prateek/dotfiles-private.git"
refreshPeriod = "168h"
{{- end }}
