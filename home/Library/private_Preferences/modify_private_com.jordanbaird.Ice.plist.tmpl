#!/usr/bin/env bash
exec '{{ .chezmoi.sourceDir }}/../scripts/macos/plist-merge' \
  --bundle-id 'com.jordanbaird.Ice' \
  --desired-b64 '{{ includeTemplate "com.jordanbaird.Ice.plist.tmpl" . | b64enc }}'
