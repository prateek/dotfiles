<!-- fork-notice:begin (managed by setup-downstream-fork; do not edit) -->
# Fork notice (Claude Code)

This repository is a **downstream fork** of [`$upstream_owner/$upstream_repo`]($upstream_url), maintained with auto-sync + LLM conflict resolution.

Before editing, read [`.fork/AGENTS.md`](.fork/AGENTS.md) — it's the full contract this skill follows. `CLAUDE.md` at repo root mirrors `AGENTS.md` so Claude Code and generic agents find the same pointer; the in-depth guidance lives in `.fork/`.

Repo-local skills are auto-discovered at `.claude/skills` (→ `../.fork/skills`). Use them rather than rediscovering conventions.
<!-- fork-notice:end -->
