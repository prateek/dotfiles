# Baseline arms

These plain Markdown files are comparison arms, not installable skills:

- `previous-local-skill.md`: the local skill replaced by this work.
- `chronosphere-skill.md`: the overlapping work-marketplace skill being
  disabled.

Run each local eval with identical fixture Git config. Compare the arms against
the live `SKILL.md`; do not attribute managed-config behavior to skill text.
