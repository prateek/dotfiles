{{- $f := includeTemplate "features.tmpl" . | fromJson -}}
# Managed by chezmoi from machines.toml wiki_host_alias. The session-archive
# sync (wiki-agent-sessions repo) fails closed when host_alias is empty.
host_alias = "{{ dig "wiki_host_alias" "" $f }}"
