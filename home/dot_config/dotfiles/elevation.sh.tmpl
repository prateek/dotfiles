# shellcheck shell=sh
# Generated by chezmoi from chezmoi.toml ([data.elevation]). Sourced by
# dotfiles_admin_elevate in home/.chezmoitemplates/script_lib.sh. Override per
# shell with the matching DOTFILES_* env var; rerun `chezmoi init` (or edit
# ~/.config/chezmoi/chezmoi.toml) to change persistent values.

: "${DOTFILES_ELEVATION_METHOD:={{ .elevation.method }}}"
: "${DOTFILES_JAMF_POLICY_ID:={{ .elevation.jamf_policy_id }}}"
