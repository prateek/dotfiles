Create a throwaway git repo under `/tmp` with a `main` branch and two commits.
Branch off to `feature-one`, add a commit, then branch off that to
`feature-two` and add another commit. Stay on `feature-two`.

Open a crit review for the work on `feature-two` and give me the URL.
