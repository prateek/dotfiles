// ABOUTME: SwiftUI app entry point for __APP_NAME__.
// ABOUTME: Provides the minimal executable surface so the scaffolded project builds before feature work starts.

import SwiftUI

@main
struct __APP_NAME__App: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
