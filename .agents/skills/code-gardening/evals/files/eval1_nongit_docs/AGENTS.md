# Docs Workspace — Agent Guide

This is my personal docs workspace. It is **not** a git repository.

## Folders

The top-level folders are:
- `drafts/` — work-in-progress writing
- `published/` — finalized essays
- `references/` — collected source material

## How to Work Here

When adding new material, place it in the folder that matches its stage.

## Folder List

The top-level folders in this workspace:
- `drafts/` — work-in-progress writing
- `published/` — finalized essays
- `references/` — collected source material

## Migration Notes (Q4 2025)

During the Q4 2025 reorg we moved all `inbox/*.md` files into `drafts/` and retired the `inbox/` folder. Going forward, new capture goes directly into `drafts/`. This migration is complete — remaining cleanup is tracked in `drafts/todo.md`.

## Style

Keep titles in sentence case. No trailing whitespace.
